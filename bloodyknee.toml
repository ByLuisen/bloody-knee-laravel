[install]
installCommand = "composer install --optimize-autoloader --no-dev && npm ci"

[build]
builder = "nixpacks"
buildCommand = "npm run prod && php artisan config:cache && php artisan route:cache && php artisan view:cache"

[start]
startCommand = "php artisan migrate --force && php artisan db:seed --force"
